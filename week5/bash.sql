-- how to see how many observations in the table
wc -l *csv

-- if we assign a name to variable
name = "patty"
echo name -- returns name

echo $name -- returns patty (which is what we stored it as)

$PATH --tries to find the path for python
which python -- finds the path to python
which -a python -- all paths to python
which $SHELL -- finds the path to shell
which -a $SHELL -- all paths to shell
date -- prints the date
today = $(date) -- puts date into the today variable
echo $today -- prints out date that was stored in that variable

-- additional info in week three folder labeled 'theloop.sh'

